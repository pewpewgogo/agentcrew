services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: agentcrew
      POSTGRES_PASSWORD: agentcrew
      POSTGRES_DB: agentcrew_test
    ports:
      - "54329:5432"
    tmpfs:
      - /var/lib/postgresql/data
